<!doctype html>
<html lang="en">
<head>
  <title>{{title}}</title>

  <meta charset="utf-8">
  <meta name="description" content="Estella González Madison's personal website">
  <meta name="author" content="Estella Madison">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  
  <link href="//fonts.googleapis.com/css2?family=Libre+Baskerville:ital,wght@0,400;0,700;1,400&display=swap" rel="stylesheet">

  <!-- capture the CSS content as a Nunjucks variable -->
  {% set css %}
    {% include "css/main.css" %}
  {% endset %}
  <!-- feed it through our cssmin filter to minify -->
  <style>
    {{ css | cssmin | safe }}
  </style>
</head>
<body>
  {{ content | safe }}
</body>
</html>